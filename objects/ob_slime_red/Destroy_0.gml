event_inherited();
/*

draw_sprite(sp_slime_red_ko, 1, x, y)

instance_create_depth(x, y, depth, ob_xp);
	if (scr_chance(.5)){
		instance_create_depth(x+random_range(-4, 4), y+random_range(-4, 4), depth +1, ob_health)
	}	
	


/// Drop xp orb



/*
draw_sprite(sp_slime_red_ko, 0, x, y);
image_speed = 1;
instance_create_depth(x, y, depth, ob_xp);
